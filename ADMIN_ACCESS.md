# 🔐 Доступы к админ-панели

## Учетные данные

**Email:** `admin@roon.local`  
**Password:** `admin123`

## URL админ-панели

**Локально (Docker):** http://localhost:8080/admin

## Первый вход

1. Откройте браузер и перейдите на http://localhost:8080/admin
2. Введите email: `admin@roon.local`
3. Введите пароль: `admin123`
4. Нажмите "Войти"

## Что можно делать в админ-панели

### Письма (Letters)
- Просмотр всех писем от пользователей
- Фильтрация по статусу оплаты (pending/paid)
- Фильтрация по дате создания
- Удаление писем
- Просмотр полного текста письма

### Предсказания (Predictions)
- Создание новых предсказаний
- Редактирование существующих предсказаний
- Установка категории (Rune/Scroll)
- Редактирование контента через rich text editor
- Активация/деактивация предсказаний
- Просмотр статистики активных предсказаний
- Фильтрация по категории и статусу активности

## Безопасность

⚠️ **Важно:** После первого входа рекомендуется изменить пароль администратора!

Для изменения пароля используйте:
```bash
docker-compose exec app php artisan tinker
```

Затем выполните:
```php
$user = App\Models\User::where('email', 'admin@roon.local')->first();
$user->password = Hash::make('новый_пароль');
$user->save();
```
